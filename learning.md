- every react app should have some important files in the src folder
    - index.js - the entry point of the app

- `rafce` means - react arrow functional component export

- `npm install --legacy-peer-deps`